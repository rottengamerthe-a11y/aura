{
  "scripts": {
    "register": "npm --prefix discord-bot run register",
    "test:premium": "npm --prefix discord-bot run test:premium --"
  },
  "dependencies": {
    "express-session": "^1.19.0"
  }
}
